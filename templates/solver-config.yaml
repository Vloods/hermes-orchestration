# Solver is a separate named profile; this file does not configure the default profile.
model:
  provider: "{{PROVIDER}}"
  default: "{{SOLVER_MODEL}}"
agent:
  reasoning_effort: high
delegation:
  orchestrator_enabled: false
  max_spawn_depth: 1
  max_concurrent_children: 1
  subagent_auto_approve: false
